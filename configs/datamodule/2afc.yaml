_target_: lfads_torch.datamodules.BasicDataModule
datafile_pattern: ${relpath:datasets/2afc*.h5}
batch_keys: []  # No additional trial metadata keys available
attr_keys: []  # No additional trial metadata keys available
batch_size: 256  # Adjust based on your data size and GPU memory